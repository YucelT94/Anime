<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yucelterlemezoglu.anime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DeathNoteActivity" />
        <activity android:name=".FairyTailActivity" />
        <activity android:name=".NarutoActivity" />
        <activity android:name=".OnePieceActivity" />
        <activity android:name=".TokyoGhoulActivity"></activity>
    </application>

</manifest>
